/** @examplens/solid — WIP. See packages/vanilla for reference implementation. */
export { collectVariableSlots, parseLensBlock, parseThemes, parseUi } from "@examplens/core";
export type { LensBlockDocument, LensDefinition, ThemesDocument, UiDocument } from "@examplens/core";

// MultiLensCodeBlock Solid component: planned — port from amdphreak.github.io prototype.
