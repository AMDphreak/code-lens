# @code-lens/flutter

**Status:** planned · **Glass lens:** native (`BackdropFilter` + lens sweep)

Dart package scaffold — load JSON5 spec via `json5` or prebuilt assets. See [docs/glass-lens-capabilities.md](../../docs/glass-lens-capabilities.md).
