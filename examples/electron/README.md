# Electron example

**Status:** planned · **Glass lens:** webview (full via `@code-lens/vanilla`)

Chromium renderer supports `backdrop-filter` — ship the same `<code-lens>` bundle as the web demo.

## Steps (future)

1. BrowserWindow loading Vite-built demo or embedded HTML
2. Optional: IPC to load lens JSON5 from workspace files

See [docs/glass-lens-capabilities.md](../docs/glass-lens-capabilities.md).
