# .NET MAUI implementation

**Status:** planned · **Glass lens:** webview (WebView2 / BlazorWebView)

Host the vanilla web component in a MAUI `WebView` or Blazor hybrid page. Windows WebView2 supports backdrop blur for embedded CSS — use runtime probe; on platforms without blur, use fallback morph.

See [docs/glass-lens-capabilities.md](../../docs/glass-lens-capabilities.md).
