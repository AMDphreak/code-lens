# Jetpack Compose implementation

**Status:** planned · **Glass lens:** native

Animate a blurred pill (`Modifier.blur()` + gradient) across diff-token `Layout` bounds. Use `spec/ui.json5` for pass duration and line stagger.

Fallback when blur unavailable on API level: cross-fade only.

See [docs/glass-lens-capabilities.md](../../docs/glass-lens-capabilities.md).
