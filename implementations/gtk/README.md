# GTK 4 implementation

**Status:** planned

| Host | Glass lens tier |
|------|-----------------|
| **WebKitGTK** | webview — embed `@code-lens/vanilla` |
| **Gtk native** | native when blur/compositing APIs are available |

Parse spec with `json-glib` or bundled JSON5 converter. GtkBlur or CSS `backdrop-filter` in WebKit when supported.

See [docs/glass-lens-capabilities.md](../../docs/glass-lens-capabilities.md).
